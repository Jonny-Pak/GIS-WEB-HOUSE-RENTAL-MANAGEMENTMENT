# Django
SECRET_KEY=replace-me
DEBUG=1

# Database (optional override)
DB_BACKEND=postgresql
DB_NAME=quanlythuenha
DB_USER=postgres
DB_PASSWORD=123456
DB_HOST=localhost
DB_PORT=5432

# Mailtrap API mode (demo/testing only)
MAILTRAP_API_TOKEN=your-mailtrap-api-token
DEFAULT_FROM_EMAIL=hello@demomailtrap.co
SUPPORT_INBOX_EMAIL=thuenhanhanh.gis@gmail.com
SUPPORT_CONTACT_PHONE=0900 123 456

# Recommended for OTP to external emails:
# set SMTP credentials from your Mailtrap Inbox.
# If EMAIL_HOST_USER and EMAIL_HOST_PASSWORD are set, SMTP is used automatically.
EMAIL_HOST=live.smtp.mailtrap.io
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=1
EMAIL_USE_SSL=0
